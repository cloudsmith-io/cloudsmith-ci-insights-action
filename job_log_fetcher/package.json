{
  "name": "job-log-fetcher",
  "version": "1.0.0",
  "description": "Fetch logs for a specific job in a workflow run and expose as file + outputs.",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx ncc build index.js -o dist --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}
